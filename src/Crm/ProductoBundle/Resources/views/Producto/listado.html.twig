<tbody>
    {% for entity in entities %}
        <tr>
            <td class="oculto">{{ entity.id }}</td>
            <td><a href="{{ path('producto_show', { 'id': entity.id }) }}">{{ entity.nombre }}</a></td>
            <td>
            <a href="{{ path('producto_show', { 'id': entity.id }) }}" class="btn yellow btn-outline">Ver</a>
                                        
            <a href="{{ path('producto_edit', { 'id': entity.id }) }}" class="btn blue btn-outline">Editar</a>
            </td>
        </tr>
    {% endfor %}
</tbody>