<tbody>
    {% for entity in entities %}
        <tr>
            <td class="oculto">{{ entity.id }}</td>
            <td><a href="{{ path('termino_show', { 'id': entity.id }) }}">{{ entity.producto }}</a></td>
            <td>{{ entity.termino }}</td>
            <td>
            <a href="{{ path('termino_show', { 'id': entity.id }) }}" class="btn yellow btn-outline">Ver</a>
                                        
            <a href="{{ path('termino_edit', { 'id': entity.id }) }}" class="btn blue btn-outline">Editar</a>
            </td>
        </tr>
    {% endfor %}
</tbody>