{% extends '::frontend.html.twig' %}

{% block body -%}
    
    <div class="row">
        <div class="col-xs-12 col-sm-12 com-md-12">
            <h2><a href="{{ path('tipolicencia') }}">
               Tipo Licencia:
                </a>
                Vista 
            </h2>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="portlet light portlet-fit ">
                <div class="portlet-title">
                    <div class="caption">
                        <i class="icon-eye font-yellow"></i>
                        <span class="caption-subject font-yellow bold uppercase">Caracteristicas</span>
                    </div>
                </div>
                <div class="portlet-body">
                    <div class="table-scrollable">
                        <table class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Precio</th>
                                    <th>Meses</th>
                                    <th>Termino</th>
                                    <th>Producto</th>
                                    <th colspan="2">Acciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>{{ entity.precio }}</td>
                                    <td>{{ entity.meses }}</td>
                                    <td>{{ entity.terminoLicencia }}</td>
                                    <td>{{ entity.producto }}</td>
                                    <td><a href="{{ path('tipolicencia_edit', { 'id': entity.id }) }}" class="btn blue btn-outline">
                                             Editar
                                        </a></td>
                                    <td>{{ form(delete_form) }}</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
