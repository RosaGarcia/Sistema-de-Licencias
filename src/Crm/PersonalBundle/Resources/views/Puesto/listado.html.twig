<tbody>
    {% for entity in entities %}
        <tr>
            <td class="oculto"><a href="{{ path('puesto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.puesto }}</td>
            <td>
                <a href="{{ path('puesto_show', { 'id': entity.id }) }}"class="btn yellow btn-outline">Ver</a>
                                        
                <a href="{{ path('puesto_edit', { 'id': entity.id }) }}" class="btn blue btn-outline">Editar</a>
            </td>
        </tr>
    {% endfor %}
</tbody>