{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MemoriaRam list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marca</th>
                <th>Modelo</th>
                <th>Capacidad</th>
                <th>Unidad</th>
                <th>Tipo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('memoriaram_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.marca }}</td>
                <td>{{ entity.modelo }}</td>
                <td>{{ entity.capacidad }}</td>
                <td>{{ entity.unidad }}</td>
                <td>{{ entity.tipo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('memoriaram_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('memoriaram_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('memoriaram_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
