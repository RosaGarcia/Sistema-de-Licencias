{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EspecificacionesEquipo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numerointerfaces</th>
                <td>{{ entity.numeroInterfaces }}</td>
            </tr>
            <tr>
                <th>Numerousb</th>
                <td>{{ entity.numeroUsb }}</td>
            </tr>
            <tr>
                <th>Expansion</th>
                <td>{{ entity.expansion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('especificacionesequipo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('especificacionesequipo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
