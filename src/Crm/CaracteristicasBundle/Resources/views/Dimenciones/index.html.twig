{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Dimenciones list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dimenciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('dimenciones_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.dimenciones }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('dimenciones_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('dimenciones_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('dimenciones_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
